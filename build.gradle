apply plugin: 'java'
apply plugin: 'application'

version = '1.0'

mainClassName = "no.finn.Anagram"

repositories {
    mavenCentral()
}

run {
    args System.getProperty("args").split()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}